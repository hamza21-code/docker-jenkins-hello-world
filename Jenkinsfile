Jenkinsfile (Declarative Pipeline)
pipeline {
    agent {
        node {
          label 'maven'
        }
      }
    stages {
        stage('Build') {
            steps {
                bat 'set'
            }
        }
    }
}